// Your code goes here
const howLongToMyBirthday = require('../src/birthday.service');

describe('Birthday Reminder', () => {
  it('Should be async', function(done) {
    //arrange
    const notADate = 'Mellon';

    // howLongToMyBirthday(notADate).then(function(result) {
    //   expect(result).toThrow(new Error(`Wrong argument!`));
    //   done();
    // });
  });
})